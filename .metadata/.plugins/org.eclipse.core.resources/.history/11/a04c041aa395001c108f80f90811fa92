import java.util.Scanner;
import java.lang.Math;

public class Ex8 
{

	public static void main(String[] args) 
	{
		int number = (int) ((20)* Math.random() + 1);
		Scanner myObj = new Scanner(System.in); 
		System.out.println("Enter a numbber between 1-20: " + number); //user input
		int guess = myObj.nextInt(); //records input
	
		while (number != guess)
		{
			System.out.println("Enter a numbber between 1-20: "); //user input
			guess = myObj.nextInt(); //records input
		}
	
		System.out.println("You won!");
		myObj.close();
	}
}
